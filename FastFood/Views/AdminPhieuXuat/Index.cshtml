
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}

@using FastFood.Models
@model IEnumerable<PHIEUXUAT>

<div class="table-agile-info">
    <div class="panel panel-default">
        <div class="panel-heading">
            PHIẾU XUẤT HÀNG
        </div>
        <div>
            <button class="btn btn-outline-primary"><a href="@Url.Action("Add", "AdminPhieuXuat")"><i class="fas fa-plus-circle" style="font-size: 150%"></i></a></button>

            <form action="Search" method="post">
                <button type="submit" class="btn btn-outline-primary sel" style="float: right">Search</button>
                <input type="text" class="form-control search" placeholder=" Search" style="float: right" name="MaNguoiDung">
            </form>
        </div>

        <div>
            <table class="table" ui-jq="footable" ui-options='{
        "paging": {
          "enabled": true
        },
        "filtering": {
          "enabled": true
        },
        "sorting": {
          "enabled": true
        }}'>
                <thead>
                    <tr>
                        <th data-breakpoints="xs">Mã Phiếu Xuất</th>
                        <th>Ngày Tạo</th>
                        <th>Lượt Tao</th>
                        <th>Ghi Chú</th>
                        <th>Mã Nguyên Liệu</th>
                        <th>Mã Người Dùng</th>
                        <th colspan="2">Chức Năng</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (PHIEUXUAT item in Model)
                    {
                        <tr data-expanded="true">
                            <td>@item.MaPhieuXuat</td>
                            <td>@item.NgayTao</td>
                            <td>@item.LuotTaoTrongNgay</td>
                            <td>@item.GhiChu</td>
                            <td>@item.MaNguyenLieu</td>
                            <td>@item.MaNguoiDung</td>
                            <td><a href="/AdminPhieuXuat/Delete/@item.MaPhieuXuat"><i class="fa fa-trash" style="font-size: 150%"></i></a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>









