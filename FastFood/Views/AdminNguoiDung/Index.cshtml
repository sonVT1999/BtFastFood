
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}

@using FastFood.Models
@model IEnumerable<NGUOIDUNG>

<div class="table-agile-info">
    <div class="panel panel-default">
        <div class="panel-heading">
            NGƯỜI DÙNG
        </div>
        <div>
            <button class="btn btn-outline-primary"><a href="@Url.Action("Add", "AdminNguoiDung")"><i class="fas fa-plus-circle" style="font-size: 150%"></i></a></button>

            <form action="Search" method="post">
                <button type="submit" class="btn btn-outline-primary sel" style="float: right">Search</button>
                <input type="text" class="form-control search" placeholder=" Search" style="float: right" name="Ten">
            </form>
        </div>

        <div>
            <table class="table" ui-jq="footable" ui-options='{
        "paging": {
          "enabled": true
        },
        "filtering": {
          "enabled": true
        },
        "sorting": {
          "enabled": true
        }}'>
                <thead>
                    <tr>
                        <th data-breakpoints="xs">Mã người Dùng</th>
                        <th>Tên</th>
                        <th>Vị Trí</th>
                        <th>Tài Khoản</th>
                        <th>Mật Khẩu</th>
                        <th>Ngày Sinh</th>
                        <th>Mã Cửa Hàng</th>
                        <th colspan="2">Chức Năng</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (NGUOIDUNG item in Model)
                    {
                        <tr data-expanded="true">
                            <td>@item.MaNguoiDung</td>
                            <td>@item.Ten</td>
                            <td>@item.ViTri</td>
                            <td>@item.TaiKhoan</td>
                            <td>@item.MatKhau</td>
                            <td>@item.NgaySinh</td>
                            <td>@item.MaCuaHang</td>
                            <td><a href="/AdminNguoiDung/Edit/@item.MaNguoiDung"><i class="fas fa-edit" style="font-size: 150%"></i></a></td>
                            <td><a href="/AdminNguoiDung/Delete/@item.MaNguoiDung"><i class="fa fa-trash" style="font-size: 150%"></i></a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>









